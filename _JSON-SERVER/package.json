{
	"name": "server",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "json-server --watch -p 4010 courses.json",
		"dev:cv": "json-server --watch -p 5001 cv.json",
		"dev:tasks": "json-server --watch -p 4011 tasks.json"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"json-server": "^0.16.3"
	}
}
